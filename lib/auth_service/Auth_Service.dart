/* import 'package:flutter/material.dart';

class CustUser {
  final String uid;
  final String email;
  final String username;
  // final String password;

  const CustUser({
    @required this.uid,
    this.email,
    this.username,
    // this.password,
  });
}

abstract class AuthService {
  Future<CustUser> signInWithEmailAndPassword(String email, String password);
  Future<CustUser> createUserWithEmailAndPassword(
      String email, String password);
  // Future<CustUser> currentUser();
  Stream<CustUser> get authStateChanges;
  Future<void> sendPasswordResetEmail(String email);
  Future<void> signOut();
  void dispose();
}
 */
